import Head from "next/head";

type Props = {};

const register = (props: Props) => {
  return (
    <div>
      <Head>
        <title>Register</title>
      </Head>
      register
    </div>
  );
};

export default register;
