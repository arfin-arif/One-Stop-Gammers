/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  // nextConfig,
  images: {
    domains: ["i.ibb.co", "image.tmdb.org", "wp.clutchpoints.com"],
  },
};
